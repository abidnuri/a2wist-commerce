import React from 'react';

const UserHistory = () => {
    return (
        <div className="text-center">
            This is a user history component
        </div>
    );
};

export default UserHistory;